<% provide :title, t("categoryindex.title") %>
<div class="col-md-4">
  <ul class="categories">
    <%= render @categories %>
  </ul>
  <% if current_user.admins? %>
    <%= link_to t("categoryindex.create"), new_admin_category_path, class: "btn btn-primary" %>
  <% end %>
</div>
