<% provide :title, @title %>
<div class="row">
  <div class="col-md-8">
    <h3><%= t "user.title" %></h3>
    <% if @users.any? %>
      <ul class="users follow">
        <%= render @users %>
      </ul>
      <%= will_paginate @users %>
    <% end %>
  </div>
</div>
